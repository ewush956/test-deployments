
services:
  dashboard:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONPATH=/app
    volumes:
      - .:/app
      - uploads:/app/data/uploads
    command: ["shiny", "run", "app/main.py", "--host", "0.0.0.0", "--port", "8000", "--reload"]
volumes:
  uploads:
